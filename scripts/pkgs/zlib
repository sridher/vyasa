# zlib
# ------------------------------
cd /builds
rm -rf zlib-1.3.1
tar -xf /sources/zlib-1.3.1.tar.xz
mkdir zlib-1.3.1/build -p
cd zlib-1.3.1/build

../configure --prefix=/usr

make -j16
make install
# ------------------------------
